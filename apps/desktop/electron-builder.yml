appId: dev.pix.app
productName: Pix
asar: true
files:
  - dist/**
  - package.json
directories:
  output: release/app
  buildResources: build
# CI packages unsigned platform dirs (`pnpm package` → electron-builder --dir).
# Identity null keeps mac builds green without Developer ID secrets.
# Icons live in directories.buildResources (./build). Paths are relative to that folder.
mac:
  identity: null
  icon: icon.icns
  target:
    - dir
  category: public.app-category.developer-tools
win:
  icon: icon.png
  target:
    - dir
  artifactName: ${productName}-${version}-${os}-${arch}.${ext}
linux:
  icon: icon.png
  target:
    - dir
  category: Development
asarUnpack:
  - "**/*.node"
  - "**/@silvia-odwyer/photon-node/**"
